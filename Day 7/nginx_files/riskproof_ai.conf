server {
    listen 80;

    server_name riskproof-ai;

    root /home/ubuntu/RiskProofAI/client;
    index index.html;

    location /api/ {
        rewrite ^/api(.*) $1 break;
        proxy_pass http://127.0.0.1:5000;
    }
}
